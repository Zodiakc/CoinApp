import React from "react";
import styles from "./ProfileInfo.module.scss";
const ProfileInfo = () => {
    return <aside className={styles.profileBlock}>Profile</aside>;
};

export default ProfileInfo;
